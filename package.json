{
  "name": "workbox-generatesw-sandbox1",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "serve": "http-server ./docs -o / -a localhost",
    "gensw": "node ./generatesw.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "devDependencies": {
    "http-server": "^14.1.1",
    "workbox-build": "^6.5.4"
  }
}
